import trigonometric.py

class function:
        def __init__(String: function):
            self.f = function
            n = trigonometric.trigonometric("sin(x)")
            self.dictionary = n.return_dictionary()
            self.pos = 0
            self.identity = parse_function()
            return

        def parse_function(self):
            count = 0
            for functions in dictionary:
                if functions == self.f:
                    self.pos = count
                    identity = self.dictionary[self.pos]
                    return identity
                if count == dictionary.length:
                    return "function does not exist"
            count +=1



